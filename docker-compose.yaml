services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fraud_model
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - .venv/